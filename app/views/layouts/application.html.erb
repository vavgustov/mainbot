<!DOCTYPE html>
<html>
<head>
  <title>Swarm</title>
  <%= csrf_meta_tags %>
  <%= stylesheet_pack_tag 'application' %>
</head>
<body>
<%= render 'shared/navbar' %>

<section class="section">
  <div class="container is-fluid">
    <%= render_notifications if !flash.empty? %>
    <%= yield %>
  </div>
</section>

<%= javascript_pack_tag 'application' %>
</body>
</html>
