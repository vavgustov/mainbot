<%= form_with(model: retailer, local: true) do |form| %>
  <% if retailer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(retailer.errors.count, "error") %> prohibited this retailer from being saved:</h2>

      <ul>
      <% retailer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.text_field :title, id: :retailer_title, class: 'input', placeholder: :title %>
  </div>

  <div class="field">
    <%= form.text_field :url, id: :retailer_url, class: 'input', placeholder: :url %>
  </div>

  <div class="field">
    <%= form.label :day %>
    <%= form.select(:day, Date::DAYNAMES.map(&:downcase)) %>
  </div>

  <div class="field">
    <%= form.label :active %>
    <%= form.check_box :active, id: :retailer_active %>
  </div>

  <div class="actions">
    <%= form.submit class: 'button is-primary' %>
  </div>
<% end %>
